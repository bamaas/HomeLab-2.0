---
controller:
  hostNetwork: true

  service:
    enabled: true
    type: LoadBalancer
    annotations:
      metallb.universe.tf/allow-shared-ip: ingress-address-pool

  # TODO: enable metrics
  metrics:
    enabled: false
    serviceMonitor:
      enabled: false
