---
# dnsmasq:
#   customDnsEntries:
#     - address=/nas/192.168.178.10

#   customCnameEntries:
#     - cname=foo.nas,nas

persistentVolumeClaim:
  enabled: true

serviceWeb:
  type: ClusterIP

serviceDns:
  loadBalancerIP: 192.168.8.100
  # annotations:
    # metallb.universe.tf/allow-shared-ip: pihole
  type: LoadBalancer

doh:
  enabled: true
  pullPolicy: Always
  envVars: {
    DOH_UPSTREAM: "https://1.1.1.1/dns-query"
  }

ingress:
  enabled: true
  hosts:
    - pihole.kubernetes.lan.basmaas.nl
